{
  "name": "release-notes-generator",
  "version": "1.0.0",
  "description": "Generate release notes from GitHub PRs and Issues using Anthropic API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
